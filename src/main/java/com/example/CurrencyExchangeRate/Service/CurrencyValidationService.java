package com.example.CurrencyExchangeRate.Service;

//generate the currency validation service interface with methods to validate currency codes and returns void
public interface CurrencyValidationService {
    void validateCurrencyCode(String currency_code);
}
